name: projects
meta_only: true
soft_deps:
    - characters
    - images
schema:
    id: uuid
    name: string
    characters:
        - character_id
        - character_id
    background:
        type: solid
        color: 255, 255, 255
